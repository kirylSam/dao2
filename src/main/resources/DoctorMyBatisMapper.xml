<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "org.DAO.DAOLayer.interfaces.DoctorMyBatisMapper">
    <select id = "get" parameterType="Integer" resultType = "Doctor">
        SELECT doctor_id as doctorId,
        first_name as firstName,
        last_name as lastName,
        salary as salary,
        department_id as departmentId,
        specialization_id as specializationId
        FROM doctors
        WHERE doctor_id = #{doctorId}
    </select>

    <select id = "getAll" resultType = "Doctor">
        SELECT doctor_id as doctorId,
        first_name as firstName,
        last_name as lastName,
        salary as salary,
        department_id as departmentId,
        specialization_id as specializationId
        FROM doctors
    </select>

    <insert id = "insert" parameterType="Doctor" keyProperty="doctorId" useGeneratedKeys="true">
        INSERT INTO doctors (first_name, last_name, salary, specialization_id, department_id)
        VALUES (#{firstName}, #{lastName}, #{salary}, #{specializationId}, #{departmentId})
    </insert>

    <update id = "update" parameterType="Doctor" keyProperty="doctorId" useGeneratedKeys="true">
        UPDATE doctors SET first_name = #{firstName},
        last_name = #{lastName},
        salary = #{salary},
        department_id = #{departmentId},
        specialization_id = #{specializationId}
        WHERE doctor_id = #{doctorId};
    </update>

    <delete id = "delete" parameterType="Doctor">
        DELETE FROM doctors WHERE doctor_id = #{doctorId};
    </delete>
</mapper>